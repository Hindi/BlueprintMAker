using UnityEngine;
using System.Collections;

public class ResolutionHandler : MonoBehaviour {

	// Use this for initialization
	void Start () {
		/*Resolution[] resolutions = Screen.resolutions;
		print("using : " + 	resolutions[resolutions.Length - 1].width + "x" + resolutions[resolutions.Length - 1].height);
		Screen.SetResolution(resolutions[resolutions.Length - 1].width, resolutions[0].height, true);*/	
		//Screen.fullScreen = false;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
